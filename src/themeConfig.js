import { createTheme } from "@mui/material/styles";
import { lightGreen, lightBlue, red } from "@mui/material/colors";

const appTheme = createTheme({
  palette: {
    primary: {
      main: red[900]
    },
    secondary: {
    main: red[600]
      },
  },

  
});



export default appTheme;
